# Creating tuple using()
t = (1, 2, 4, 5)
print(t[0])
# Tuples cannot be updated
# t[0]=(1) this will throw error

# tuple with only one element should look like this
t1 = (1,)
# not like this
# t1 = (1)
print(t1)
